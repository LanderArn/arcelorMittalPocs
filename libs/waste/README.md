# waste

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build waste` to build the library.

## Running unit tests

Run `nx test waste` to execute the unit tests via [Jest](https://jestjs.io).
